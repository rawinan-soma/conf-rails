<div class="form-control mb-4 read-only-field">
  <label class="label-text font-medium mb-1 block"><%= @label %></label>
  <div class="px-3 py-2 rounded bg-cream-100" data-read-only style="background-color: var(--color-cream-100); color: var(--color-ink-2);">
    <%= @value %>
  </div>
</div>
