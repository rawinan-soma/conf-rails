<% content_for :title, t(".title") %>

<div class="hero min-h-screen">
  <div class="hero-content text-center">
    <div class="max-w-md">
      <h1 class="text-3xl font-bold"><%= t(".heading") %></h1>
      <p class="py-6 text-base-content/70">
        <%= t(".signed_in_as", email: current_user&.email) %>
      </p>
    </div>
  </div>
</div>
