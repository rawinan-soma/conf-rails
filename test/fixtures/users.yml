# Story 1.3 ATDD: User fixtures
#
# UIDs use example.test domain and non-realistic patterns to avoid gitleaks false positives.
# No real PII — all emails use the example.test domain.
# These fixtures are loaded once Story 1.3 Task 1 (User model + migration) is implemented.

regular_user:
  provider: openid_connect
  uid: "test-uid-regular-001"
  email: regular@example.test
  admin: false
  profile_completed_at: ~

admin_user:
  provider: openid_connect
  uid: "test-uid-admin-001"
  email: admin@example.test
  admin: true
  profile_completed_at: 2026-01-01 00:00:00
